import React from 'react';

const CurrentLocation = ({ zoomIn }) => {
  return(
    <button className="currLoc">Become a host!</button>
  )
};

export default CurrentLocation;

/* browser's built in function : to check if the browser has a geolocation*/